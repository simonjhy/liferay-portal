buildService {
	apiDir = "../sample2-api/src/main/java"
}

dependencies {
	compile project(":modules:sample2:sample2-api")

	compileOnly group: "com.liferay.portal", name: "release.portal.api"
}

group = "sample2"