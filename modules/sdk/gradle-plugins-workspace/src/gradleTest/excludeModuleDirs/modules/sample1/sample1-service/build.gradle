dependencies {
	compile project(":modules:sample1:sample1-api")

	compileOnly group: "com.liferay.portal", name: "release.portal.api"
}

buildService {
	apiDir = "../sample1-api/src/main/java"
}

group = "sample1"